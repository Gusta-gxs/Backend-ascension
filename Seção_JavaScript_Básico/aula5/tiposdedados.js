const nome = 'Gustavo'; // string
const nome1 = "Gustavo"; // string
const nome2 = `Gustavo`; // string
const num1 = 10; // number
const num2 = 1.35; //number
let nomeAluno; // undefined = não aponta para local nenhum na memória
const sobrenomeAluno = null; // nulo = não aponta para local nenhum na memória
const aprovado = true; // Boolean = true ou false, só tem duas possibilidades de valor, mas é um valor lógico.

let a = 2;
let b = a;
console.log(a, b); // 2, 2

a = 3;
console.log(a, b); // 3, 2 , o b mantém o valor acima dele.

/*
const array1 = [1, 2];
const array2 = array1;

console.log(array1, array2)

array2.push(3, 5, 22);
console.log(array1, array2);
*/

